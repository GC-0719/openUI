{
  "name": "openui-example-mcp-postgres",
  "private": true,
  "version": "0.0.0",
  "description": "Prisma sample schema for openUI MCP wizard — see README.md",
  "scripts": {
    "schema:print": "cat schema.prisma"
  }
}
