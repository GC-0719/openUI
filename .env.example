# Optional Anthropic key at dev-server startup (also configurable in-app)
OPENUI_AI_KEY=
ANTHROPIC_API_KEY=

# Enable Angular workspace + preview in the studio (set by npm run dev / dev:angular)
OPENUI_ANGULAR=
VITE_OPENUI_ANGULAR=

# Production static server port (server.js)
PORT=3000
