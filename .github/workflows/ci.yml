name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      # Gates on errors; non-correctness warnings remain as a cleanup backlog.
      - name: Lint
        run: npm run lint
      - name: Test
        run: npm test
      - name: Build (React studio)
        run: npm run build:react
      - name: Build (@openui/react package)
        run: npm run build --prefix kits/react/template
      - name: Build (@openui/angular package)
        run: npm run build --prefix kits/angular/template
